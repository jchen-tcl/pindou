# 更新记录

历史版本按时间倒序排列；当前功能与使用方式见 [README](README.md)。

## v1.0.14 Release Notes

- 新增“拼豆成品”预览，以圆形带孔豆粒展示摆放效果，空格留空，手动调整后同步刷新。
- 在成品视图点击“保存效果图”可保存圆形拼豆预览；图纸数据与 Excel 保持原有格子格式。此为熨烫前摆放示意。

## v1.0.13 Release Notes

- 编辑入口精简为“单格”和“移动”，默认选择单格；保留选色、缩放、撤销重做和保存，移除其它改色工具入口。
- 手动调整新增“单格”工具：选色后轻点，只修改一个像素格（对应一颗豆子）；拖动、跨格释放和双指操作不改色。
- 当前格子增加双色边框高亮，结合行列和色号定位；每次单格修改可独立撤销。

## v1.0.12 Release Notes

- 结果页新增“手动调整”：支持画笔、连通填色、全图同色替换、擦除及撤销/重做。仅可选择进入编辑时图纸已有的颜色。
- 支持双指缩放、移动和“放大编辑”，适配最大 1000×1000 及长方形图纸；空格显示棋盘底纹，不等同于白色豆子。
- 编辑在独立草稿上进行，完成后同步更新用豆量、坐标、效果图及 Excel；返回时有未保存修改会询问保存或放弃。图纸尺寸不变，不重新裁边。
- 撤销最多保留 20 步、合计不超过 200 万条格子变更，超限淘汰最早记录；不保存跨重启草稿。全空图纸需要保留至少一颗豆子才能完成。
- 验证：本地编辑逻辑、页面事件、导出与百万格性能测试；使用微信自带编译器校验 WXML/WXSS。模拟器联调需要开启开发者工具服务端口，真机触摸操作仍需设备验证。

## v1.0.11 Release Notes

- 参数页默认开启“自动裁掉四周白边”，可手动关闭。生成后裁掉外围完整白色行列，保留主体内部白色、奶黄色等浅色和独立细节。
- 按裁剪后的实际宽高显示网格、成品尺寸及用豆统计，并显示减少的颗粒数；预览保留长宽比例，Excel 保留正方形格子。
- 全白图片保持原尺寸；主体范围内残留的背景白色仍计入用豆量。

## v1.0.10 Release Notes

- 切换到 221 或 291 色卡时，“最多使用”自动设为对应的最大色数。
- 首次进入参数页默认使用色卡最大色数，仍可手动调低；返回参数页保留已保存的设置。

## v1.0.9 Release Notes

- 新增 256×256、512×512 预设和自定义正方形网格，每边支持 1～1000 整数；非法输入会提示修改。
- 大图采样控制在一百万像素以内，减少转换和 Excel 导出的临时内存；小网格保留三倍采样。
- 大图预览隐藏过密网格线和色号；Excel 仍保留完整格数、色号与正方形单元格。
- 本地测试覆盖 256、512、777、1000 网格的转换与统计，以及最大尺寸的 Excel 单元格数量。微信模拟器和真机性能尚未验证。

## v1.0.8 Release Notes

- 修复浅奶黄色主体被合并成白色的问题：浅色区域合并时额外检查色度差异，保留浅色填充与白色背景的区别。
- 算法更新为 `flat-v3`，旧图纸需重新转换。
- 本地原图验证覆盖 32、48、64、128 网格；新增 221/291 色卡奶黄色保留、浅粉浅蓝保留与近白噪点清理回归测试。微信模拟器运行效果仍需重新编译验证。

## v1.0.7 Release Notes

- 修复 Excel 图纸格子呈长方形的问题：补齐工作簿及工作表视图，避免高 DPI 下行高被 Excel 缩小。
- 统一列宽换算基准；效果图单格为 13.5×13.5 磅，带色号单格为 27×27 磅，色号文字仍为 10 磅。
- 修改对重新导出的 Excel 生效，已导出的旧文件需重新导出。
- 已知待排查：模拟器中观察到 221 色卡把蓝色配成紫色，本地同输入未复现。已增加原色到色号的诊断与两套色卡切换回归测试，尚未确认运行时根因。

## v1.0.6 Release Notes

- 修复 128×128 下两采样点宽的过渡色带被误认为主色的问题：检查半径随采样尺寸调整，并结合两侧颜色判断是否属于混合边缘。
- 算法版本更新为 `flat-v2`，旧图纸需重新转换。独立的小面积颜色、纯灰色块和细彩色线条仍受保护。
- 最新转换的诊断元数据保存在小程序用户目录 `bead-diagnostics.json`，仅保存尺寸、色数和算法版本等信息，每次覆盖，不保存图片和矩阵。

## v1.0.5 Release Notes

- 默认启用纯色配色算法，优先读取原图，合并近似色并保护细黑线和眼睛高光。
- 首页显示 v1.0.5；结果页显示该图纸实际生成版本和“纯色优化”。旧算法图纸会返回参数页提示重新转换。
- 开发者工具控制台搜索 `[bead-plan]` 可核对实际算法 `flat-v1`、采样尺寸、主色数、是否回退及最终色数；日志不包含图片路径或内容。

## v1.0.4 Release Notes

- 新增 128×128 网格选项，共 16,384 颗拼豆，预计成品尺寸约 64×64 cm。
- MARD 221/291 色卡的颜色数量上限会随所选色卡动态调整。
- 切换色卡时自动限制超出范围的颜色数量，避免参数与色卡不一致。
- 增加 128×128 转换、统计和 Excel 导出的回归验证。
