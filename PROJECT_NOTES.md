# 拼豆转换器项目备注

更新日期：2026-09-09。本文件记录当前实现与交接状态；项目介绍见 README.md，各版本变更见 CHANGELOG.md。

## 当前状态

- 项目目录：`D:\Desktop\pindou`，微信小程序，原生 JS / WXML / WXSS。
- 当前代码版本：`v1.0.14`；纯色算法版本：`flat-v3`。版本统一来自 `utils/version.js`。
- Git 仓库：`jchen-tcl/pindou`，分支 `main`。最近已确认推送的提交为 `d3aecc3`，对应 v1.0.10。
- 本轮提交包含 v1.0.11～v1.0.14 功能及文档整理。实际提交与远程状态以 Git 为准；尚未确认这些版本上传微信后台。

## 当前用户流程与功能

1. 上传图片，进入参数页选择 MARD 221 / 291 色卡。
2. 切换色卡时，最多用色自动设为 221 / 291；用户仍可调低。首次进入使用最大值，返回时保留已保存参数。
3. 预设网格：32、48、64、128、256、512；自定义正方形网格每边支持 1～1000 整数。
4. 默认开启自动裁白边：生成后去掉外围完整白色行列，保留主体内部白色和奶黄色等浅色。全白图保持原尺寸；主体包围框内的背景仍计豆数。
5. 结果页支持原图、效果图、效果图加色号、拼豆成品四种预览。成品模式显示圆形带孔豆粒，属于熨烫前摆放示意。
6. 手动调整目前只开放“单格”和“移动”，默认单格。支持已有色号选色、缩放、行列定位、高亮、撤销重做、完成和取消。
7. 单格操作只修改一颗豆子；拖动、跨格释放和双指缩放不改色。旧版画笔、填色、替换、擦除逻辑仍留在代码及测试中，但入口已移除。
8. 保存手动修改后，更新用豆量、各色坐标、预览缓存和 Excel。编辑不改变图纸宽高、不再次裁白边。
9. 导出 Excel 包含效果图、效果图加色号、拼豆统计，图纸单元格为正方形。在拼豆成品视图点击保存效果图，会保存圆形成品预览。

## 关键实现与数据约定

| 模块 | 位置与职责 |
| --- | --- |
| 参数与生成 | `pages/params/params.js`、`utils/bead.js`：参数校验、采样、配色、裁边及统计 |
| 配色 | `utils/flat.js`、`utils/quantize.js`、`utils/cleanup.js`：纯色合并、Oklab 匹配、碎点清理 |
| 网格和裁边 | `utils/grid.js`、`utils/trim.js`：网格上限、采样大小、外围白边识别 |
| 手动编辑 | `pages/editor/`、`utils/editor.js`、`utils/editor-view.js`：草稿、单格操作、历史、坐标和视口 |
| 预览和导出 | `pages/result/result.js`、`utils/bead-preview.js`、`utils/exporter.js` |
| 色卡数据 | `data/mard/colors.js` 与 `colors.json` 保持同步；`utils/colors.js` 选择套装 |
| 诊断 | `utils/diagnostics.js`：控制台 `[bead-plan]` 和用户目录 `bead-diagnostics.json` |

- `plan.matrix` 为二维矩阵，`0` 是空格，正数为色卡索引加一。白色豆子也必须使用正数色号。
- `gridSize` 是原始采样网格边长；裁边后以 `gridWidth/gridHeight` 或矩阵实际宽高为准，不能假定结果仍为正方形。
- `total` 为非空颗粒数；`detail` 为实际颜色及数量；`cellsByColor` 的键为色卡零基索引，坐标从 1 开始。
- `trimmedBeads` 表示裁去的外围格数，`emptyCells` 表示编辑图纸里的空格，两者含义不同。
- 编辑仅使用进入编辑时已有颜色，草稿与原图隔离。撤销最多 20 步、总计 200 万条格子变化，超出时淘汰最早记录。
- 当前任务存在 `getApp().globalData.taskData` 中，没有跨重启编辑草稿恢复。
- 小图最多三倍采样，总采样控制在一百万像素以内；编辑用视口绘制，整张矩阵不传给 `setData`。
- 圆形预览按颜色复用豆粒小图；大图分批绘制，保存或重新编辑后通过版本标识阻止旧异步结果覆盖新缓存。

## 验证与调试

- 全量测试命令：`node --test tests/*.test.js`。
- 2026-09-09 文档整理时全量运行 66 项测试通过，包含单格及圆形预览测试。后续代码修改后应重新验证。
- 已用微信开发者工具附带的 WXML/WXSS 编译器做模板检查；本地 Canvas 检查了长方形编辑、百万格缩略图和圆形豆粒绘制。
- `scripts/compare-image.py` 用于本地原图配色比较；`scripts/verify-editor-render.js` 用于编辑画布检查。它们不能替代微信模拟器或真机验证。
- 开发者工具服务端口最近检查仍关闭，CLI 自动化无法连接。需在“设置 → 安全设置”开启服务端口后继续联调。
- 曾出现 `/pages/editor/editor.wxml not found`：文件和注册均存在，完整模板编译包含该页，怀疑热重载缓存。已在本机 `project.private.config.json` 关闭 `compileHotReLoad`；该问题尚无用户确认已恢复。
- 新增页面后建议清缓存并完整编译，必要时关闭项目重新打开。
- 微信原生返回手势下的未保存提示、画布触摸定位、不同手机性能及相册保存仍需要模拟器/真机验证。

## 待办与已知限制

- **内容安全**：用户曾收到微信内容安全整改通知。当前尚未完成内容安全后端接入，不能宣称已整改；需要确定云环境或后端，再核对图片检测接口、异步结果及发布场景拦截。
- **221 / 291 色差**：曾在模拟器观察到 221 将蓝色配成紫色，本地相同原图未复现。已有诊断信息与共享颜色一致性测试，运行时根因尚未确认。
- **轮廓缺口**：用户提过头部/手部轮廓不完整，随后明确暂不处理；没有新增自动补线算法。
- **精简编辑内部代码**：用户只需要单格改色，目前移除了其它工具入口，后续可清理不可达的旧工具逻辑和相应页面测试。
- 大尺寸成品预览在手机整图视图下豆粒很小；目前没有单独的成品预览放大交互。
- 本地性能测试结果不代表手机表现；最大图纸的转换、编辑、圆形预览和 Excel 导出需在实际设备验收。

## 发布前检查

1. 确认新增编辑页四个文件、工具模块和 `app.json` 页面注册全部纳入提交。
2. 运行全量测试，清缓存完整编译，实际走通生成 → 单格修改 → 完成 → 成品预览 → 图片/Excel 保存。
3. 检查长方形图纸、白色豆子、已有空格，以及最大尺寸图纸。
4. 记录实际上传版本与结果；Git 推送不等于微信上传，更不等于审核通过或发布。

历史推送曾遇到 GitHub SSH 22 端口连接中断，使用 443 端口并以本机已信任的 `github.com` 主机密钥校验后成功；未修改仓库 origin 地址。
